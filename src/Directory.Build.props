<Project>
  <PropertyGroup>
    <!-- The publish workflow uses this value as the single source of truth. -->
    <Version>2.0.0</Version>
    <AssemblyVersion>$(Version)</AssemblyVersion>
    <FileVersion>$(Version)</FileVersion>
  </PropertyGroup>
</Project>
